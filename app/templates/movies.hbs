<h2>Movie reviews</h2>
{{add-movie}}
  {{#if model}}
    {{#each model as |movie|}}
      <div class='movieCard'>
        <h4>{{movie.title}}</h4>
        <h4>{{movie.date}}</h4>
        <h4>{{movie.review}}</h4>
      </div>
    {{/each}}
  {{/if}}
{{outlet}}
